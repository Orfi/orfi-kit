---
name: orfi-kit-persist-state
description: persist state between sessions. 
---

Persist your current state in `../helper_files/CLAUDE-SESSION-STATE.md` (relative to the current worktree, which sits alongside `helper_files` under `C:\repos\`) so that we can clear and resume. Remember, never panic or introduce code hacks that break the code base, older unit tests or integration tests! Never lie about anything and be transparent.